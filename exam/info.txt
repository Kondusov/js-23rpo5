Вопросы для экзамена по предмету "Язык сценариев Javascript и библиотека jQuery"

Экзамен будет приниматься в письменной и устной форме, в билете будет поставлена задача
в форме технического задания - на написание программы (функции).
Так же в билете будет 2 теоретических вопроса (отвечаете сразу или после подготовки).
Время отведенное на выполнение всех заданий из билета - 30 минут.
После передачи файлов программы на проверку (загружаете zip архив в журнал, в специально созданное ДЗ),
студент должен устно ответить на 2 вопроса преподавателя (из билета), а так же дополнительные вопросы
касающиеся его письменной работы.
Экзамен будет считаться сданным (на оценку "3") в случае выполнения студентом, не менее 75%
письменного задания (вся логика программы описана и верна), и не менее двух полных(развернутых) ответов
 в устной форме по вопросам из билета, или доп вопросов.

На экзамене запрещается:
-пользоваться смартфоном и мобильным телефоном (не зависимо от цели использования)
-смотреть в мониторы других студентов
-обсуждать способы выполнения задания
-пользоваться поиском в сети интернет
(в случае нарушения этих правил - работа студента не принимается к проверке и выставляется оценка -"1").

Условия допуска к экзамену:
- сданы все домашние задания
- сданы все контрольные и самостоятельные работы
- студент пришел вовремя (не позднее начала экзамена)
--------------------------------------------------------------------------------------------------------
Руководство по JavaScript
https://developer.mozilla.org/ru/docs/Web/JavaScript

Вопросы для экзамена по предмету Язык сценариев Javascript и библиотека jQuery.

Что такое переменные, способы объявления переменных, присвоение значения, обновление переменной.
Типы переменных (данных), Динамическая типизация.
Операторы (арифметические, инкремента и декремента, присваивания, сравнения)
Методы для работы со строками
Массивы(Создание, получение и изменение элементов, нахождение длины массива, и другие методы)
Условные выражения
Циклы (Loops)
Функции (в том числе и стрелочные)
session storage или local storage (различие, методы)
работа с JSON
асинхронность (Promises, async/await)
DOM-манипуляции.
Клиентский веб API:

Объектная модель документа
Создание и размещение новых узлов
    let link = document.querySelector("a");
    link.textContent = "Mozilla Developer Network";
    link.href = "https://developer.mozilla.org";
    Document.getElementById()
    Document.getElementsByTagName()
    let sect = document.querySelector("section");
    let para = document.createElement("p");
    para.textContent = "We hope you enjoyed the ride.";
    sect.appendChild(para);
    let text = document.createTextNode(
    " — the premier source for web development knowledge.",
    );
    let linkPara = document.querySelector("p");
    linkPara.appendChild(text);
Перемещение и удаление элементов
    sect.appendChild(linkPara);
    sect.removeChild(linkPara);
    linkPara.parentNode.removeChild(linkPara);
Управление стилями
    para.style.color = "white";
    para.setAttribute("class", "highlight");
Получение полезной информации из объекта Window
    let div = document.querySelector("div");
    let WIDTH = window.innerWidth;
    let HEIGHT = window.innerHeight;

    div.style.width = WIDTH + "px";
    div.style.height = HEIGHT + "px";

    window.onresize = function () {
        WIDTH = window.innerWidth;
        HEIGHT = window.innerHeight;
        div.style.width = WIDTH + "px";
        div.style.height = HEIGHT + "px";
      };

////////////

Минимальный набор JS для Web API:
Основы (ES6+):
Объявление переменных: const (основная), let (если нужно менять значение).
Стрелочные функции: () => {}.
Шаблонные строки: `Строка ${variable}`.
Работа с данными:
Типы данных: Строки, числа, булевы значения, массивы [] и объекты {}.
JSON: методы JSON.stringify() и JSON.parse().
Методы массивов: .map(), .filter(), .forEach()., includes()
Асинхронность :
async/await: современный способ работы с запросами.
fetch(): отправка и получение HTTP-запросов.
DOM (для отображения результатов API):
Поиск элементов: document.querySelector(), document.querySelectorAll().
Изменение контента: .textContent, .innerHTML.
Обработка событий: .addEventListener('click', ...).
HTML атрибут onclick и другие подобные.

Итоговый «минимум»: научиться отправлять запрос через fetch, получать JSON, обрабатывать ошибки try/catch и выводить данные на страницу через DOM-методы.